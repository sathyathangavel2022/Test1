apiVersion: apps/v1
kind: Deployment
metadata:
  name: sathya-jenkins
  namespace: batch2
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sathya-jenkins
  template:
    metadata:
      labels:
        app: sathya-jenkins
    spec:
      containers:
      - name: jenkins
        image: jenkins/jenkins:lts-jdk11
        ports:
        - containerPort: 8080
        volumeMounts:
        - name: jenkins-home
          mountPath: /var/jenkins_home
      volumes:
      - name: jenkins-home
        emptyDir: { }